# Archive from origin/master (a8a78f3)

- `storage.zip` — master-only archive blob (not used by the running app)
- `claude-settings.json` — copy of master `.claude/settings.json` (editor settings)

Do not unpack or apply these into the live app without an explicit migration plan.
